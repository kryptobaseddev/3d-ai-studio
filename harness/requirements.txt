# studio3d harness — verified to install on CPython 3.14
numpy>=2.0
trimesh>=4.5
manifold3d>=2.5      # robust manifold boolean (CSG) backend for trimesh
numpy-stl>=3.1       # fast binary STL read/write
shapely>=2.0         # 2D polygon ops for extrude/revolve/text profiles
Pillow>=10.0         # thumbnail image handling
matplotlib>=3.8      # headless thumbnail render (Agg) + text-glyph outlines
lxml>=5.0            # 3MF (XML) export
networkx>=3.0        # trimesh graph ops (repair, splitting)
scipy>=1.11          # proximity / convex ops used by trimesh
mapbox-earcut>=1.0   # robust polygon triangulation for extrusions
rtree>=1.3           # spatial index — enables ray-cast wall-thickness analysis
embreex>=2.17        # fast ray engine (optional; rtree path works without it)
PyYAML>=6.0          # user printer profiles (YAML in the XDG config dir)
# external tool (not pip): Blender (headless Workbench) for multi-view renders;
# the render step falls back to matplotlib if `blender` is not on PATH.

# dev/test only (not installed by the plugin bootstrap):
#   pytest>=8.0
